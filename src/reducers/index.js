import { routerReducer } from 'react-router-redux'
import rangeInfo from './rangeInfoReducer'
import divisorInfo from './divisorInfoReducer'

export default {
  rangeInfo,
  divisorInfo,
  routing: routerReducer
}
